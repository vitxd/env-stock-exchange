services:
  app:
    build:
      context: .
      dockerfile: ./build/Dockerfile
      target: dev
    volumes:
      - ./:/app
    command: 'pnpm run dev'
    ports:
      - 3000:3000
  db:
    image: mysql:8
    environment:
      - MYSQL_ROOT_PASSWORD=root
    volumes:
      - db-data:/var/lib/mysql

volumes:
  db-data:
